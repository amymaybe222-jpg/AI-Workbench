// Centralized localStorage keys used across the app's remaining mock-persistence hooks.
export const STORAGE_KEYS = {
  isLoggedIn: "aiw:is-logged-in",
  theme: "aiw:theme",
  cookieConsent: "aiw:cookie-consent",
} as const;
